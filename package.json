{
  "name": "revora",
  "version": "1.0.0",
  "description": "Revora application with multiple services",
  "scripts": {
    "start:landing": "cd \"LANDING PAGE\" && npm run dev",
    "start:createtrip": "cd createtrip && npm run dev",
    "start:login": "cd login && npm start",
    "dev": "concurrently \"npm run start:landing\" \"npm run start:createtrip\" \"npm run start:login\"",
    "install:all": "npm install && cd \"LANDING PAGE\" && npm install && cd createtrip && npm install && cd login && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}